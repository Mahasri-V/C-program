#include<stdio.h>

void main()
{
    int n,j;
    printf("Enter number : ");
    scanf("%d",&n);
    
    for(j=0;j<n;j++);
    }


int fib(int n)
{
    if(n<=1)
    {
        return n;
    }
    else
    {
        return fib(n-1)+fib(n-2);
    }
}
